[build]
  command = "echo 'No build command needed'"
  publish = "."

[build.environment]
  SECRETS_SCAN_OMIT_PATHS = "amp-center.html"

[functions]
  directory = "netlify/functions"

[functions."agent"]
  timeout = 26
